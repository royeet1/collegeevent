apiVersion: v1
kind: Service
metadata:
  name: collegeevent-service
spec:
  type: NodePort
  selector:
    app: collegeevent
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
    nodePort: 30081